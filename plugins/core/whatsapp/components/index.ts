/**
 * WhatsApp Plugin UI Components
 *
 * This file exports all Vue components that will be built into the UI bundle.
 * Components are loaded dynamically by the Hay dashboard at runtime.
 */

export { default as AfterSettings } from "./settings/AfterSettings.vue";
